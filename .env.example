# Model Configuration
# -------------------
# The default model to use for chat requests if not specified by the client.
DEFAULT_MODEL="qwen3:8b"

# Ollama Connection Settings
# ---------------------------
# The host and port where the Ollama server is running.
# The default values are for a standard local installation.
OLLAMA_HOST="http://localhost"
OLLAMA_PORT="11434"

# Feature Flags
# -------------
# Enable or disable agent mode (tool calling).
# Set to "true" or "1" to enable, or "false" or "0" to disable.
ENABLE_AGENT_MODE="true"
